# Copia questo file in .env.local e compila i valori reali.
# .env.local è ignorato da git.

# --- Supabase ------------------------------------------------------------
# Li trovi su: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# --- Stripe (client) -----------------------------------------------------
# Publishable key — dashboard.stripe.com → Developers → API keys
VITE_STRIPE_PUBLIC_KEY=
